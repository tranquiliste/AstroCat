name: Release Builds

on:
  push:
    tags:
      - "v*"
  workflow_dispatch:
    inputs:
      tag:
        description: "Release tag (e.g. v1.3.7-beta)"
        required: true
        type: string

permissions:
  contents: write

env:
  RELEASE_TAG: ${{ inputs.tag || github.ref_name }}

jobs:
  create-release:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Generate release summary
        id: notes
        run: |
          git fetch --tags --force
          tag="${RELEASE_TAG}"
          previous="$(git tag --sort=-creatordate | grep -Fv "$tag" | head -n 1 || true)"
          if [ -n "$previous" ]; then
            range="${previous}..${tag}"
          else
            range="${tag}"
          fi
          notes="Changes"
          while IFS= read -r line; do
            notes="${notes}\n- ${line}"
          done < <(git log --oneline "${range}")
          printf "body<<EOF\n%s\nEOF\n" "$notes" >> "$GITHUB_OUTPUT"

      - name: Create or update release
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          cat > release-notes.txt <<'EOF'
          ${{ steps.notes.outputs.body }}
          EOF
          if gh release view "${RELEASE_TAG}" >/dev/null 2>&1; then
            gh release edit "${RELEASE_TAG}" \
              --title "Selune ${RELEASE_TAG}" \
              --notes-file release-notes.txt \
              --prerelease
          else
            gh release create "${RELEASE_TAG}" \
              --title "Selune ${RELEASE_TAG}" \
              --notes-file release-notes.txt \
              --prerelease
          fi

  build-windows:
    runs-on: windows-latest
    needs: create-release
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: "3.13"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          python -m pip install -r requirements.txt
          python -m pip install pyinstaller

      - name: Build
        run: |
          python -m PyInstaller --clean --noconfirm spec/Selune-windows.spec

      - name: Package
        run: |
          Compress-Archive -Path "dist/Selune/*" -DestinationPath "Selune-Windows.zip"

      - name: Upload release asset
        env:
          GH_TOKEN: ${{ github.token }}
        run: gh release upload "${env:RELEASE_TAG}" "Selune-Windows.zip" --clobber

  build-linux:
    runs-on: ubuntu-latest
    needs: create-release
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: "3.13"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          python -m pip install -r requirements.txt
          python -m pip install pyinstaller

      - name: Build
        run: |
          python -m PyInstaller --clean --noconfirm spec/Selune-linux.spec

      - name: Package
        run: |
          cd dist
          zip -r ../Selune-Linux.zip "Selune"

      - name: Upload release asset
        env:
          GH_TOKEN: ${{ github.token }}
        run: gh release upload "${RELEASE_TAG}" "Selune-Linux.zip" --clobber

  build-macos:
    runs-on: macos-latest
    needs: create-release
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: "3.13"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          python -m pip install -r requirements.txt
          python -m pip install pyinstaller

      - name: Build
        run: |
          python -m PyInstaller --clean --noconfirm spec/Selune-macos.spec

      - name: Package
        run: |
          ditto -c -k --sequesterRsrc --keepParent \
            "dist/Selune.app" \
            "Selune-macOS-AppleSilicon.zip"

      - name: Upload release asset
        env:
          GH_TOKEN: ${{ github.token }}
        run: gh release upload "${RELEASE_TAG}" "Selune-macOS-AppleSilicon.zip" --clobber

  build-macos-intel:
    runs-on: macos-15-intel
    needs: create-release
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: "3.13"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          python -m pip install -r requirements.txt
          python -m pip install pyinstaller

      - name: Build
        run: |
          python -m PyInstaller --clean --noconfirm spec/Selune-macos.spec

      - name: Package
        run: |
          ditto -c -k --sequesterRsrc --keepParent \
            "dist/Selune.app" \
            "Selune-macOS-Intel.zip"

      - name: Upload release asset
        env:
          GH_TOKEN: ${{ github.token }}
        run: gh release upload "${RELEASE_TAG}" "Selune-macOS-Intel.zip" --clobber
